node_modules
*data.json
!cleaned_manga_data.json
*.xml
dist
.next
next-env.d.ts
.vercel
.env*

# Anime data now stored in Turso (not JSON)
# Keep cleaned_anime_data.json locally for seed script only
anime_data.json
cleaned_anime_data.json
.env
.env.*
!.env.example
.saasmaker.json
playwright-report
test-results
.open-next
.pages-deploy
.wrangler
.playwright-mcp
.DS_Store
coverage/
coverage-final.json

# Blume (docs presentation layer) — generated, not committed.
# blume dev regenerates .blume/; blume build --isolated uses .blume-verify/.
# `blume build` (without --isolated) writes dist/, which collides with the
# SPA build above — prefer --isolated or a separate checkout for publishing.
.blume/
.blume-verify/
docs-dist/

# Local agent session logs / scratch (not part of the knowledge base).
.symphony/
.clawpatch/
.agent-logs/
*.agent.log

